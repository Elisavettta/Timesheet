//
//  FileSave.swift
//  AppFinal
//
//  Created by Mac on 20.12.21.
//
/*
import Foundation
import CoreData
import UIKit

class ToDoList : NSManagedObject, Identifiable {
    @NSManaged var createdAt: Date?
 lazy var persistenContainer: NSPersistentContainer = {
        let container = NSPersistentContainer(name: "swift")
        container.loadPersistentStores(completionHandler: {
            (storeDescription,error) in
            if let error = error as NSError?{
                <#body#>
            }
        })
        
        let context = (UIApplication.shared.delegate as! Monday).persistenContainer.viewContext
    }
 //   let context = (UIApplication.shared.delegate as! Monday).persistenContainer.viewContext

}

*/
